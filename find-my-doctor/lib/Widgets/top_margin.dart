import 'package:find_my_doctor/Utils/extensions.dart';
import 'package:flutter/material.dart';

class TopMargin extends StatelessWidget {
  const TopMargin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 2.h,
    );
  }
}
